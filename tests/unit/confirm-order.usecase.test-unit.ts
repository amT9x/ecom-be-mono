import { describe, beforeEach, it, expect } from "vitest";

describe('ConfirmOrderUseCase test-unit', () => {
  beforeEach(() => {

  })
  it('should check order exists', () => {
    expect(true).toBe(true);
  });

  it('should confirm status pending', () => {
    expect(true).toBe(true);
  })

  it('should confirmed order', () => {
    expect(true).toBe(true);
  })
});
