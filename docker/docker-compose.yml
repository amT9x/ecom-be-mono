version: "3.9"
name: ecom-mono
services:
  api:
    image: ecom-mono
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "3002:3001"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    env_file:
      - ../.env.docker
    restart: unless-stopped
